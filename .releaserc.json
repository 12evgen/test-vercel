{
  "branches": [
    "+([0-9]).+([0-9]).+([0-9])",
    "main"
  ],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/exec", {
      "successCmd": "if [ -z \"${nextRelease.version}\" ]; then\n  echo \"Version not specified; Exiting.\"\n  exit 1;\nfi"
    }]
  ]
}
